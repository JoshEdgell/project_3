<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Admin Page</title>
  </head>
  <body>
    <h2>Delete Users</h2>
    <ul>
          <% for (let i = 0; i < users.length; i++) { %>
            <li><%=users[i].userName%></li>
            <form action="/admin/user/<%=users[i]._id%>?_method=DELETE" method="POST">
              <input type="submit" value="Delete User"/>
            </form>
          <% } %>
        </ul>

        <h2>Delete Jokes</h2>
        <ul>
              <% for (let i = 0; i < jokes.length; i++) { %>
                <li><%=jokes[i].joke%></li>
                <form action="/admin/joke/<%=jokes[i]._id%>?_method=DELETE" method="POST">
                  <input type="submit" value="Delete Joke"/>
                </form>
              <% } %>
            </ul>


  </body>
</html>
